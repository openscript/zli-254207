function funktion1(eins, zwei, drei) {
    return eins * zwei + drei;
}

const funktion2 = function (eins, zwei, drei) {
    return eins * zwei + drei;
}

const funktion3 = (eins, zwei, drei) => {
    return eins * zwei + drei;
}

const funktion4 = (eins, zwei, drei) => eins * zwei + drei;